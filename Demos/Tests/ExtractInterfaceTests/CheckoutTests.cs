using CommonObjects;
using ExtractInterface;
using FluentAssertions;
using Moq;
using Xunit;

namespace ExtractInterfaceTests
{
    public class CheckoutTests
    {
        //[Fact]
        //public void CreateReceipt_should_return_receipt()
        //{
        //    var checkout = new Checkout(new ReceiptRepository());

        //    var receipt = checkout.CreateReceipt(new Money(12));

        //    receipt.Should().NotBeNull();
        //}
    }
}
